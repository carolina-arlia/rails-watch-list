<div class="card-trip">
  <img src= <%= movie.poster_url %> />
  <div class="card-trip-infos">
    <div>
      <h2><%= movie.title %></h2>
      <p><%= movie.overview %></p>
      <p><%= bookmark.comment %></p>
    </div>
    <h2 class="card-trip-pricing"><%= movie.rating %><i class="fa-solid fa-star"></i></h2>
  </div>
</div>
